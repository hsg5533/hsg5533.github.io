# CLAUDE.md

## 작업 규칙

### 추측으로 코딩하지 않는다

문제(에러, 버그, 예상과 다른 동작)가 주어지면 **원인을 확인하기 전에 코드를 고치지 않는다.**

순서는 다음과 같다.

1. **조사한다.** 에러 메시지와 스택 트레이스를 읽고, 관련 파일을 확인하고,
   필요하면 웹에서 해당 라이브러리·프레임워크의 문서와 알려진 이슈를 찾아본다.
   내부 지식만으로 단정하지 않는다.
2. **원인을 확인한다.** 추정이 아니라 근거를 확보한다.
   재현하거나, 실제 값을 찍어보거나, 변환 결과를 직접 열어본다.
3. **해결 방안을 먼저 제시한다.** 무엇이 원인이고 어떻게 고칠 것인지 설명한다.
   선택지가 여럿이면 장단점과 함께 추천안을 제시한다.
4. **방향이 정해지면 그때 코딩한다.**

원인이 확인되지 않았다면 "아마 ~때문일 것"이라고 말하고 고치는 대신,
**무엇을 더 확인해야 하는지** 말한다.

#### 왜

추측으로 고치면 두 가지 손해가 난다.
멀쩡한 코드를 건드려 원인과 무관한 변경이 쌓이고,
진짜 원인은 그대로 남아 같은 문제가 다시 난다.

#### 조사용 파일

원인 확인을 위해 임시 파일(probe, 테스트 스크립트 등)을 만들었다면
프로젝트 안에 남기지 말고 스크래치패드로 옮긴다.

## 프로젝트 구조

React 19 + TypeScript + Vite 5. GitHub Pages(`gh-pages`)로 배포한다.

- `/` — [Home](src/pages/Home.tsx). 메인 포트폴리오.
  Main → Sec1(프로필) → Values(키워드) → History(경력) → Sec2(스킬)
  → Resume(이력서) → Sec3(프로젝트 12개) → Sec4(연락처)
- `/presentation` — [Presentation](src/pages/Presentation.tsx). 발표용 21장.
  `src/components/Slide1.tsx` ~ `Slide21.tsx`를 `import.meta.glob`으로 모아
  파일명의 숫자 순서대로 렌더한다.
  슬라이드를 추가하면 `Presentation.tsx`의 `.progress` nav 링크도 같이 늘려야 한다.
- `/speed`, `/wonder` — 실험 페이지.

### 역할 분담

메인은 **카탈로그**(무엇을 만들었나), 프레젠테이션은 **서사**(어떻게 일하는 사람인가).
같은 내용을 양쪽에 중복해서 넣지 않는다.

### 스타일

슬라이드 스타일은 [presentation.css](src/assets/css/presentation.css)에 모여 있다.
새 슬라이드는 기존 클래스(`.wrap`, `.keys`, `.tl-row`, `.vision`, `.likes`, `.tags`)를
먼저 재사용하고, 그래도 안 되면 그때 추가한다.

### 검증

```
npx tsc --noEmit    # 타입체크
npx vite build      # 빌드 (npm run build 는 둘 다 실행)
```
